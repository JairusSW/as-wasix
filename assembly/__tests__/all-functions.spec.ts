import { describe, it, expect } from "as-test/assembly";
import * as raw from "../bindings/wasix_32v1";
import * as api from "../api";

describe("all function exports", () => {
  it("exposes every raw wasix import", () => {
    expect(changetype<usize>(raw.callback_signal) > 0).toBe(true);
    expect(changetype<usize>(raw.clock_time_set) > 0).toBe(true);
    expect(changetype<usize>(raw.fd_dup) > 0).toBe(true);
    expect(changetype<usize>(raw.fd_dup2) > 0).toBe(true);
    expect(changetype<usize>(raw.fd_event) > 0).toBe(true);
    expect(changetype<usize>(raw.fd_pipe) > 0).toBe(true);
    expect(changetype<usize>(raw.tty_get) > 0).toBe(true);
    expect(changetype<usize>(raw.tty_set) > 0).toBe(true);
    expect(changetype<usize>(raw.getcwd) > 0).toBe(true);
    expect(changetype<usize>(raw.chdir) > 0).toBe(true);
    expect(changetype<usize>(raw.thread_spawn_v2) > 0).toBe(true);
    expect(changetype<usize>(raw.thread_sleep) > 0).toBe(true);
    expect(changetype<usize>(raw.thread_id) > 0).toBe(true);
    expect(changetype<usize>(raw.thread_join) > 0).toBe(true);
    expect(changetype<usize>(raw.thread_parallelism) > 0).toBe(true);
    expect(changetype<usize>(raw.thread_signal) > 0).toBe(true);
    expect(changetype<usize>(raw.futex_wait) > 0).toBe(true);
    expect(changetype<usize>(raw.futex_wake) > 0).toBe(true);
    expect(changetype<usize>(raw.futex_wake_all) > 0).toBe(true);
    expect(changetype<usize>(raw.thread_exit) > 0).toBe(true);
    expect(changetype<usize>(raw.stack_checkpoint) > 0).toBe(true);
    expect(changetype<usize>(raw.stack_restore) > 0).toBe(true);
    expect(changetype<usize>(raw.path_open2) > 0).toBe(true);
    expect(changetype<usize>(raw.fd_fdflags_get) > 0).toBe(true);
    expect(changetype<usize>(raw.fd_fdflags_set) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_raise_interval) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_fork) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_exec) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_exec2) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_exec3) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_spawn) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_spawn2) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_id) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_parent) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_join) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_signal) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_signals_get) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_signals_sizes_get) > 0).toBe(true);
    expect(changetype<usize>(raw.proc_snapshot) > 0).toBe(true);
    expect(changetype<usize>(raw.port_bridge) > 0).toBe(true);
    expect(changetype<usize>(raw.port_unbridge) > 0).toBe(true);
    expect(changetype<usize>(raw.port_dhcp_acquire) > 0).toBe(true);
    expect(changetype<usize>(raw.port_addr_add) > 0).toBe(true);
    expect(changetype<usize>(raw.port_addr_remove) > 0).toBe(true);
    expect(changetype<usize>(raw.port_addr_clear) > 0).toBe(true);
    expect(changetype<usize>(raw.port_mac) > 0).toBe(true);
    expect(changetype<usize>(raw.port_addr_list) > 0).toBe(true);
    expect(changetype<usize>(raw.port_gateway_set) > 0).toBe(true);
    expect(changetype<usize>(raw.port_route_add) > 0).toBe(true);
    expect(changetype<usize>(raw.port_route_remove) > 0).toBe(true);
    expect(changetype<usize>(raw.port_route_clear) > 0).toBe(true);
    expect(changetype<usize>(raw.port_route_list) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_status) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_addr_local) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_addr_peer) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_open) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_pair) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_set_opt_flag) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_get_opt_flag) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_set_opt_time) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_get_opt_time) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_set_opt_size) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_get_opt_size) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_join_multicast_v4) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_leave_multicast_v4) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_join_multicast_v6) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_leave_multicast_v6) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_bind) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_listen) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_accept_v2) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_connect) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_recv_from) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_send_to) > 0).toBe(true);
    expect(changetype<usize>(raw.sock_send_file) > 0).toBe(true);
    expect(changetype<usize>(raw.resolve) > 0).toBe(true);
    expect(changetype<usize>(raw.epoll_create) > 0).toBe(true);
    expect(changetype<usize>(raw.epoll_ctl) > 0).toBe(true);
    expect(changetype<usize>(raw.epoll_wait) > 0).toBe(true);
    expect(changetype<usize>(raw.dl_invalid_handle) > 0).toBe(true);
    expect(changetype<usize>(raw.dlopen) > 0).toBe(true);
    expect(changetype<usize>(raw.dlsym) > 0).toBe(true);
    expect(changetype<usize>(raw.call_dynamic) > 0).toBe(true);
    expect(changetype<usize>(raw.closure_prepare) > 0).toBe(true);
    expect(changetype<usize>(raw.closure_allocate) > 0).toBe(true);
    expect(changetype<usize>(raw.closure_free) > 0).toBe(true);
    expect(changetype<usize>(raw.reflect_signature) > 0).toBe(true);
  });

  it("exposes every namespace api function", () => {
    expect(changetype<usize>(api.clock.timeSet) > 0).toBe(true);
    expect(changetype<usize>(api.fs.getcwd) > 0).toBe(true);
    expect(changetype<usize>(api.fs.changedir) > 0).toBe(true);
    expect(changetype<usize>(api.fs.dup) > 0).toBe(true);
    expect(changetype<usize>(api.fs.dup2) > 0).toBe(true);
    expect(changetype<usize>(api.fs.event) > 0).toBe(true);
    expect(changetype<usize>(api.fs.pipe) > 0).toBe(true);
    expect(changetype<usize>(api.fs.openAt) > 0).toBe(true);
    expect(changetype<usize>(api.fs.fdFlagsGet) > 0).toBe(true);
    expect(changetype<usize>(api.fs.fdFlagsSet) > 0).toBe(true);
    expect(changetype<usize>(api.tty.get) > 0).toBe(true);
    expect(changetype<usize>(api.tty.set) > 0).toBe(true);
    expect(changetype<usize>(api.thread.spawnV2) > 0).toBe(true);
    expect(changetype<usize>(api.thread.sleep) > 0).toBe(true);
    expect(changetype<usize>(api.thread.id) > 0).toBe(true);
    expect(changetype<usize>(api.thread.join) > 0).toBe(true);
    expect(changetype<usize>(api.thread.parallelism) > 0).toBe(true);
    expect(changetype<usize>(api.thread.signal) > 0).toBe(true);
    expect(changetype<usize>(api.thread.futexWait) > 0).toBe(true);
    expect(changetype<usize>(api.thread.futexWake) > 0).toBe(true);
    expect(changetype<usize>(api.thread.futexWakeAll) > 0).toBe(true);
    expect(changetype<usize>(api.thread.exit) > 0).toBe(true);
    expect(changetype<usize>(api.asyncRuntime.callbackSignal) > 0).toBe(true);
    expect(changetype<usize>(api.asyncRuntime.epollCreate) > 0).toBe(true);
    expect(changetype<usize>(api.asyncRuntime.epollCtl) > 0).toBe(true);
    expect(changetype<usize>(api.asyncRuntime.epollWait) > 0).toBe(true);
    expect(changetype<usize>(api.jump.checkpoint) > 0).toBe(true);
    expect(changetype<usize>(api.jump.restore) > 0).toBe(true);
    expect(changetype<usize>(api.proc.raiseInterval) > 0).toBe(true);
    expect(changetype<usize>(api.proc.fork) > 0).toBe(true);
    expect(changetype<usize>(api.proc.exec) > 0).toBe(true);
    expect(changetype<usize>(api.proc.exec2) > 0).toBe(true);
    expect(changetype<usize>(api.proc.exec3) > 0).toBe(true);
    expect(changetype<usize>(api.proc.spawn) > 0).toBe(true);
    expect(changetype<usize>(api.proc.spawn2) > 0).toBe(true);
    expect(changetype<usize>(api.proc.id) > 0).toBe(true);
    expect(changetype<usize>(api.proc.parent) > 0).toBe(true);
    expect(changetype<usize>(api.proc.join) > 0).toBe(true);
    expect(changetype<usize>(api.proc.signal) > 0).toBe(true);
    expect(changetype<usize>(api.proc.signalsGet) > 0).toBe(true);
    expect(changetype<usize>(api.proc.signalsSizesGet) > 0).toBe(true);
    expect(changetype<usize>(api.proc.snapshot) > 0).toBe(true);
    expect(changetype<usize>(api.subprocess.spawn) > 0).toBe(true);
    expect(changetype<usize>(api.subprocess.spawn2) > 0).toBe(true);
    expect(changetype<usize>(api.subprocess.join) > 0).toBe(true);
    expect(changetype<usize>(api.subprocess.signal) > 0).toBe(true);
    expect(changetype<usize>(api.net.bridge) > 0).toBe(true);
    expect(changetype<usize>(api.net.unbridge) > 0).toBe(true);
    expect(changetype<usize>(api.net.dhcpAcquire) > 0).toBe(true);
    expect(changetype<usize>(api.net.addrAdd) > 0).toBe(true);
    expect(changetype<usize>(api.net.addrRemove) > 0).toBe(true);
    expect(changetype<usize>(api.net.addrClear) > 0).toBe(true);
    expect(changetype<usize>(api.net.mac) > 0).toBe(true);
    expect(changetype<usize>(api.net.addrList) > 0).toBe(true);
    expect(changetype<usize>(api.net.gatewaySet) > 0).toBe(true);
    expect(changetype<usize>(api.net.routeAdd) > 0).toBe(true);
    expect(changetype<usize>(api.net.routeRemove) > 0).toBe(true);
    expect(changetype<usize>(api.net.routeClear) > 0).toBe(true);
    expect(changetype<usize>(api.net.routeList) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketStatus) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketAddrLocal) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketAddrPeer) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketOpen) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketPair) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketSetOptFlag) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketGetOptFlag) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketSetOptTime) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketGetOptTime) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketSetOptSize) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketGetOptSize) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketJoinMulticastV4) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketLeaveMulticastV4) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketJoinMulticastV6) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketLeaveMulticastV6) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketBind) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketListen) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketAccept) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketConnect) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketRecvFrom) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketSendTo) > 0).toBe(true);
    expect(changetype<usize>(api.net.socketSendFile) > 0).toBe(true);
    expect(changetype<usize>(api.net.resolve) > 0).toBe(true);
    expect(changetype<usize>(api.dynamic.invalidHandle) > 0).toBe(true);
    expect(changetype<usize>(api.dynamic.open) > 0).toBe(true);
    expect(changetype<usize>(api.dynamic.symbol) > 0).toBe(true);
    expect(changetype<usize>(api.dynamic.call) > 0).toBe(true);
    expect(changetype<usize>(api.dynamic.closurePrepare) > 0).toBe(true);
    expect(changetype<usize>(api.dynamic.closureAllocate) > 0).toBe(true);
    expect(changetype<usize>(api.dynamic.closureFree) > 0).toBe(true);
    expect(changetype<usize>(api.dynamic.reflectSignature) > 0).toBe(true);
  });
});
